type User {
  user_id: String!
  email: String!
  name: String!
}

type Query {
  user(user_id: String!): User
}

input NewUser {
  name: String!
}

type Mutation {
  createUser(user: NewUser!): User!
}
